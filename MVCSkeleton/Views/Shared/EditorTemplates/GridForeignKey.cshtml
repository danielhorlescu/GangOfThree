@using MVCSkeleton.Presentation.Controls
@model object
           
@(
 Html.Controls().DropDownListFor(m => m)        
        .BindTo((SelectList)ViewData[ViewData.TemplateInfo.GetFullHtmlFieldName("") + "_Data"])
)
